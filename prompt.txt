Soal 1 — Integer Overflow Detection!
Deskripsi

Baca dua bilangan bulat bertipe int.
Program harus menjumlahkan kedua bilangan tersebut.

Jika terjadi integer overflow, cetak:

OVERFLOW


Jika tidak terjadi overflow, cetak hasil penjumlahan.

⚠️ DILARANG menggunakan long, BigInteger, atau try-catch untuk mendeteksi overflow.

Input:
Soal1 a b

Output
hasil penjumlahan

Contoh Input:
Soal1 2147483647 1

Contoh Output:
OVERFLOW


Contoh Input 2:
Soal1 1000000000 1000000000

Contoh Output 2:
2000000000

Soal 2 — Float vs Double Precision Trap!
Deskripsi

Baca dua bilangan desimal.

Jumlahkan keduanya menggunakan float

Jumlahkan keduanya menggunakan double

Hitung selisih absolut dari kedua hasil tersebut

Output harus ditampilkan dengan 6 digit di belakang koma.

Input
Soal2 x y

Contoh Input
Soal2 0.1 0.2

Contoh Output
0.000000

Contoh Input
Soal2 1234567.89 0.11

Contoh Output
0.015625

Soal 3 — Primitive vs Wrapper Comparison!
Deskripsi

Baca satu bilangan bulat N.

Lakukan langkah berikut:

Simpan N ke dalam objek Integer a

Salin nilai a ke Integer b

Tambahkan 1 ke a

Bandingkan a dan b menggunakan:

operator ==

method equals()

Input
Soal3 N

Contoh Input
Soal3 127

Contoh Output
==: false
equals: false

Contoh Input
Soal3 128

Contoh Output
==: false
equals: false

Soal 4 — String Immutability & Reference!
Deskripsi

Baca satu string S.

Lakukan langkah berikut:

Buat String a = S

Buat String b = new String(S)

Gabungkan string "X" ke a

Bandingkan a dan b menggunakan:

operator ==

method equals()

Input
Soal4 S

Contoh Input
Soal4 hello

Contoh Output
==: false
equals: false

Soal 5 — Parsing & Type Safety!
Deskripsi

Baca satu bilangan bulat, satu bilangan desimal, dan satu boolean.

Lakukan langkah berikut:

Parse seluruh input ke tipe data yang sesuai

Hitung:

integer × double


Jika nilai boolean adalah false, hasil dikali -1

Cetak hasil dengan 2 digit di belakang koma

⚠️ DILARANG melakukan casting manual antar tipe.

Input
Soal5 integer double boolean

Contoh Input
Soal5 10 2.5 false

Contoh Output
-25.00
tolong kerjakan tugas ini, dan berikan komentar/kegunaan code tiap linenya



compilenya darimana?

kenapa gabisa?

kenapa file not found tulisannya

tolong buat ulang codenya, tapi semua soal langsung dibuat codenya dalam 1 file dan bisa di compile satu persatu nanti. bisa pake if else

not found

ini gimana cara compilenya

ini apa??

kenapa gabisa terus di compile

tetap couldnt find

tetap couldnt find dan gabisaa

kenapa jadi gabisa lagi??

harus compile dengan ini javac src/*.java -d bin
cd bin && java Main ini untuk menjalankan

import java.util.*;
import java.math.*;

public class Main {

    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);

        String soal = sc.next();

        switch (soal) {

            // ================= SOAL 1 =================
            case "Soal1": {

                int a = sc.nextInt();
                int b = sc.nextInt();

                // Deteksi overflow tanpa long
                if ((b > 0 && a > Integer.MAX_VALUE - b) ||
                    (b < 0 && a < Integer.MIN_VALUE - b)) {

                    System.out.println("OVERFLOW");
                } else {
                    System.out.println(a + b);
                }

                break;
            }

            // ================= SOAL 2 =================
            case "Soal2": {

                double x = sc.nextDouble();
                double y = sc.nextDouble();

                float fSum = (float) x + (float) y;
                double dSum = x + y;

                double diff = Math.abs(fSum - dSum);

                System.out.printf("%.6f\n", diff);

                break;
            }

            // ================= SOAL 3 =================
            case "Soal3": {

                int N = sc.nextInt();

                Integer a = N;
                Integer b = a;

                a = a + 1;

                System.out.println("==: " + (a == b));
                System.out.println("equals: " + a.equals(b));

                break;
            }

            // ================= SOAL 4 =================
            case "Soal4": {

                String S = sc.next();

                String a = S;
                String b = new String(S);

                a = a + "X";

                System.out.println("==: " + (a == b));
                System.out.println("equals: " + a.equals(b));

                break;
            }

            // ================= SOAL 5 =================
            case "Soal5": {

                String sInt = sc.next();
                String sDouble = sc.next();
                String sBool = sc.next();

                int i = Integer.parseInt(sInt);
                double d = Double.parseDouble(sDouble);
                boolean b = Boolean.parseBoolean(sBool);

                double result = i * d;

                if (!b) {
                    result *= -1;
                }

                System.out.printf("%.2f\n", result);

                break;
            }

        }

        sc.close();
    }
}  ini code ku, 
ini compile: javac src/*.java -d bin
ini jalan: cd bin && java Main
lihat tampilan pada gambar. punyaku gabisa di compile" apa yang salah??

compilenya gimana?? masi sama kayak gini??ini compile: javac src/*.java -d bin
ini jalan: cd bin && java Main